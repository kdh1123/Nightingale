import { useQuery, useQueryClient } from "@tanstack/react-query";
import { useEffect, useState } from "react";
import { StatePanel } from "../../shared/components/StatePanel";
import { MiniChart, SectionHeader, SeverityBadge } from "../../shared/components/Visuals";
import { cleanupSecurityLogs, getApplicationSettings, getSecurityReport, listNotifications, markNotificationRead, updateApplicationSettings, type ApplicationSettings } from "./api";
const categories=["General","Monitoring","Security","Notification","Appearance","Database","Advanced"];
export function SecurityManagementPage(){const client=useQueryClient();const[category,setCategory]=useState("General");const[message,setMessage]=useState<string|null>(null);const settings=useQuery({queryKey:["settings"],queryFn:getApplicationSettings});const report=useQuery({queryKey:["security-report"],queryFn:getSecurityReport});const notifications=useQuery({queryKey:["notifications"],queryFn:listNotifications,refetchInterval:5000});useEffect(()=>{if(settings.data)document.documentElement.dataset.theme=settings.data.uiTheme},[settings.data]);if(settings.isPending||report.isPending)return <StatePanel title="보안 관리 준비 중">설정과 리포트를 불러오고 있습니다.</StatePanel>;if(settings.isError||report.isError||!settings.data||!report.data)return <StatePanel title="보안 관리 정보를 불러올 수 없습니다">잠시 후 다시 시도해 주세요.</StatePanel>;const save=async(next:ApplicationSettings)=>{await updateApplicationSettings(next);await client.invalidateQueries({queryKey:["settings"]});setMessage("설정을 저장했습니다.")};const toggle=(key:keyof ApplicationSettings)=>{const value=settings.data[key];if(typeof value==="boolean")void save({...settings.data,[key]:!value})};const rows=category==="Monitoring"?[["monitoringEnabled","Real-time monitoring","Monitor selected locations continuously"],["autoBaselineRefresh","Automatic baseline refresh","Keep known-good file data current"]] as const:category==="Security"?[["threatDetectionEnabled","Threat detection","Create local signals from suspicious changes"],["securityScoreEnabled","Security score","Calculate the device protection score"]] as const:[];return <section><SectionHeader eyebrow="Reports, preferences & local operations" title="Security management"/>{message?<p role="status">{message}</p>:null}<div className="metric-grid"><article className="card metric-card score-card"><span>SECURITY SCORE</span><strong>{report.data.securityScore.score}<small>/100</small></strong></article><article className="card metric-card"><span>INCIDENTS</span><strong>{report.data.totalIncidents}</strong><p>Tracked locally</p></article><article className="card metric-card"><span>WATCHED FOLDERS</span><strong>{report.data.monitoredFolderCount}</strong><p>Protected locations</p></article><article className="card metric-card"><span>FILE EVENTS</span><strong>{report.data.fileEventCount}</strong><p>Retained signals</p></article></div><div className="dashboard-grid"><article className="card panel chart-panel"><div className="panel-title"><h2>Security report trend</h2><span>DAILY DETECTIONS</span></div><div className="chart-grid"><MiniChart label="Daily detection count" tone="amber" values={[2,1,4,2,5,3,6,2,4,3,report.data.fileEventCount%8]}/></div></article><article className="card panel"><div className="panel-title"><h2>Severity summary</h2><span>CURRENT</span></div><div className="timeline">{Object.entries(report.data.severityCounts).map(([level,count])=><div className="timeline-item" key={level}><i className="timeline-dot"/><strong>{level}</strong><SeverityBadge severity={`${count} events`}/></div>)}</div></article></div><div className="page-grid" style={{marginTop:14}}><article className="card panel"><div className="panel-title"><h2>Settings</h2><span>{category.toUpperCase()}</span></div><div className="settings-nav">{categories.map(item=><button className={category===item?"active":""} onClick={()=>setCategory(item)} key={item}>{item}</button>)}</div></article><div className="stack"><article className="card panel"><h2>{category}</h2>{rows.length?rows.map(([key,label,description])=><label className="setting-row" key={key}><span>{label}<small>{description}</small></span><input className="switch" type="checkbox" checked={settings.data[key]} onChange={()=>toggle(key)}/></label>):category==="Appearance"?<label className="setting-row"><span>Application theme<small>Follow the system or choose a fixed appearance</small></span><select className="field" value={settings.data.uiTheme} onChange={e=>void save({...settings.data,uiTheme:e.target.value as ApplicationSettings["uiTheme"]})}><option value="system">System</option><option value="light">Light</option><option value="dark">Dark</option></select></label>:category==="Database"?<><label className="setting-row"><span>Log retention<small>Keep local security logs for this many days</small></span><input className="field" type="number" min="1" max="3650" value={settings.data.logRetentionDays} onChange={e=>void save({...settings.data,logRetentionDays:Number(e.target.value)})}/></label><button className="btn secondary" onClick={()=>void cleanupSecurityLogs().then(count=>setMessage(`${count}개의 오래된 알림을 정리했습니다.`))}>Clean old logs</button></>:<p className="empty">{category} preferences will appear here as this local security profile evolves.</p>}</article><article className="card panel"><div className="panel-title"><h2>Recent notifications</h2><span>{notifications.data?.filter(x=>!x.read).length??0} UNREAD</span></div><ul className="event-list">{notifications.data?.slice(0,4).map(item=><li key={item.id}><strong>{item.title}</strong><span>{item.message}</span>{!item.read?<button className="btn secondary" onClick={()=>void markNotificationRead(item.id).then(()=>client.invalidateQueries({queryKey:["notifications"]}))}>Mark read</button>:null}</li>)??<li className="empty">최근 알림이 없습니다.</li>}</ul></article></div></div></section>}
